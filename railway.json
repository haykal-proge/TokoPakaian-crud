{
    "build": {
        "builder": "nixpacks",
        "buildCommand": "composer install --no-dev && php artisan key:generate && npm ci && npm run build"
    },
    "deploy": {
        "restartPolicyType": "ON_FAILURE",
        "restartPolicyMaxRetries": 3
    }
}
